{
  "name": "townfall-tornado",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "debug:logs": "node scripts/debug-log-server.mjs",
    "baseline:v1": "node scripts/capture-rebuild-baseline.mjs --label v1",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "verify:architecture": "node scripts/verify-foundation-architecture.mjs",
    "verify:render": "node scripts/verify-render.mjs"
  },
  "dependencies": {
    "three": "^0.181.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/three": "^0.181.0",
    "typescript": "^6.0.3",
    "vite": "^7.3.6"
  }
}
